public class Solution {
    public int bulbSwitch(int n) {
        return (int)Math.sqrt(n);  //need odd number times to toggle. only possible if odd divisors. only possible in square numbers
        
    }
}
