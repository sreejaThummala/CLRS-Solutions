public void reverse(int[] arr, int k){
  int n = arr.length;
  
  for(int i = 0;  i < n; i +=k){
  
  int left = 0;
  int right = Math.min(i+k-1, n-1);
  
  while(left<right){
    swap(arr,left++,right--);
  }
  }
}
